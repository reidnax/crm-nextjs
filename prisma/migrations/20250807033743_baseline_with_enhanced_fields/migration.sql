-- CreateTable
-- This is a baseline migration created after restoring from crmdb_backup.sql
-- and applying enhanced schema fields using prisma db push.
-- All tables and enhanced fields are already present in the database.

-- Baseline migration - no changes needed as schema was applied via db push